{
  "name": "checkout-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "pactTest": "export NODE_ENV=pactTest && jest --testRegex \"/*(.test.pact.js)\"",
    "publish:pact": "node ./publish.js"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "devDependencies": {
    "@pact-foundation/pact": "^8.2.4",
    "axios": "^0.21.1",
    "jest": "^24.8.0"
  }
}
